# ─────────────────────────────────────────────────────────────────────────────
# Cortexa AI · Dependencias completas (post-auditoría)
# Pin con upper-bound para que un major release no rompa la app sin previo aviso.
# ─────────────────────────────────────────────────────────────────────────────

# ── Core web ────────────────────────────────────────────────────────────────
streamlit>=1.40,<2.0
streamlit-authenticator>=0.4.2,<0.5
fastapi>=0.115.0,<0.120
uvicorn[standard]>=0.30.0,<0.40
python-multipart>=0.0.9,<1.0
httpx>=0.27,<1.0
pydantic>=2.7,<3.0

# ── LangChain stack ─────────────────────────────────────────────────────────
langchain>=1.2.15,<2.0
langchain-classic>=1.0.3,<2.0
langchain-community>=0.4.1,<1.0
langchain-core>=1.2.10,<2.0
langchain-experimental>=0.3.0,<1.0
langchain-ollama>=0.2.0,<1.0
langchain-qdrant>=0.2.0,<1.0
langgraph>=0.2,<1.0

# ── Vector DB / embeddings ──────────────────────────────────────────────────
qdrant-client>=1.9.0,<2.0
sentence-transformers>=3.0.0,<5.0
rank-bm25>=0.2.2,<1.0

# ── Procesamiento documental ────────────────────────────────────────────────
pdfplumber>=0.11.0,<1.0
pypdf>=4.3.1,<6.0
docx2txt>=0.8,<1.0

# ── Datos ───────────────────────────────────────────────────────────────────
pandas>=2.2.2,<3.0
sqlalchemy>=2.0,<3.0

# ── Seguridad / cripto / auth ───────────────────────────────────────────────
bcrypt>=4.1.3,<5.0
cryptography>=42.0,<46.0
pyotp>=2.9,<3.0
qrcode[pil]>=7.4,<9.0

# ── Conectores ──────────────────────────────────────────────────────────────
google-auth>=2.30,<3.0
google-auth-oauthlib>=1.2,<2.0
google-api-python-client>=2.140,<3.0

# ── Infra ──────────────────────────────────────────────────────────────────
apscheduler>=3.10.0,<4.0
pyyaml>=6.0.2,<7.0

# ── Evaluación RAG ─────────────────────────────────────────────────────────
ragas>=0.2,<1.0

# ── (Solo para script de migración una sola vez; eliminar después) ──────────
# langchain-chroma>=1.1.0,<2.0
# chromadb>=1.5.7,<2.0
